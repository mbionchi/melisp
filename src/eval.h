#ifndef ML_EVAL_H
#define ML_EVAL_H
/*
#include "list.h"
#include "parse.h"

struct env {
    node_t *pairs;
};
typedef struct env env_t;

struct pair {
    char *sym;
    tree_node_t(*f);
};
typedef struct pair pair_t;

void eval_ast(tree_node_t**, env_t*);
*/
#endif
